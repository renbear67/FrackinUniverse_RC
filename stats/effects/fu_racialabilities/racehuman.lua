function init()
	effect.addStatModifierGroup({
		{stat = "maxEnergy", amount = 75},
		{stat = "maxHealth", amount = 50}
	})
		
  script.setUpdateDelta(0)
end

function update(dt)

end

function uninit()
  
end