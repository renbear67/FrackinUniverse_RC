function init()
	effect.addStatModifierGroup({
		{stat = "maxEnergy", amount = 60},
		{stat = "maxHealth", amount = 30}
	})
		
  script.setUpdateDelta(0)
end

function update(dt)

end

function uninit()
  
end